# ============================================================
# Deteccion de Armas - Requirements (YOLOv8 + ONNX/NCNN)
# Licencias: AGPL-3.0 (ultralytics) / Apache 2.0 / BSD / MIT
# Entrenamiento: laptop con RTX 5050 (CUDA 12.x)
# Despliegue: Raspberry Pi 5 (ONNX Runtime / NCNN)
# ============================================================

# --- YOLO (modelo + entrenamiento + export) ---
ultralytics>=8.3.0

# --- Vision y procesamiento ---
opencv-python>=4.9.0
Pillow>=10.0.0
numpy>=1.24.0

# --- Dataset Open Images v7 ---
fiftyone>=0.23.0

# --- Export (ONNX para Raspberry Pi) ---
onnx>=1.15.0
onnxruntime>=1.17.0
onnxslim>=0.1.34

# --- Reconocimiento facial (identificacion de personas) ---
insightface>=0.7.3
# onnxruntime ya esta arriba; en laptop con GPU usar en su lugar:
#   pip install onnxruntime-gpu

# --- Alertas ---
pygame>=2.5.0

# --- Utilitarios ---
pyyaml>=6.0.0
tqdm>=4.66.0
matplotlib>=3.8.0

# --- Notas de instalacion ---
# 1. Instalar PyTorch con CUDA 12.x ANTES de este requirements:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# 2. En la Raspberry Pi 5 NO se instala este archivo. Solo se necesita:
#    pip install opencv-python onnxruntime numpy pygame pyyaml
